#include <stdio.h>
#include <string.h>

int main() {
	int n, m, cnt = 0;
	scanf("%d %d", &n, &m);

	char **name = NULL;
	name = (char**)malloc(sizeof(char*) * 20);
	for (int i = 0; i < 20; i++) {
		name[i] = (char*)malloc(sizeof(char) * (n + m));
	}

//	char name[1000000][20];
	for (int i = 0; i < n + m; i++) {
		scanf("%s", name[i]);
	}

	int tmp = (int*)malloc(sizeof(int) * (n + m));
	int tmp2 = 0;

	for (int i = 0; i < n; i++) {
		for (int j = 0; j < m; j++) {
			if (strcmp(name[i], name[j + n]) == 0) {
//				printf("%s\n", name[i]);
				cnt++;
			}
		}
	}
	printf("%d\n", cnt);
	for (int i = 0; i < n; i++) {
		for (int j = 0; j < m; j++) {
			if (strcmp(name[i], name[j + n]) == 0) {
				printf("%s\n", name[i]);
			}
		}
	}
}
